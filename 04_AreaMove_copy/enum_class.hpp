#ifndef ___ENUM__CLASS
#define ___ENUM__CLASS

namespace debrisys {

enum class ERotDire { LEFT, RIGHT, };

enum class EStraDire { FRONT, BEHIND };

}   // namespace debrisys

#endif // ___ENUM__CLASS
